import 'package:flutter/material.dart';

/// Use `AppTheme.lightTheme()` e `AppTheme.darkTheme()` em `MaterialApp`.
class AppColors {
  // Primary brand
  static const Color primary = Color(0xFF6C63FF);
  static const Color primaryContainer = Color(0xFFEAE7FF);

  // Accent / secondary
  static const Color secondary = Color(0xFF00BFA6);
  static const Color secondaryContainer = Color(0xFFE6F7F2);

  // Background / surface
  static const Color background = Color(0xFFF6F7FB);
  static const Color surface = Color(0xFFFFFFFF);

  // Text / icons
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color(0xFF00332E);
  static const Color onSurface = Color(0xFF111827);

  // Misc
  static const Color error = Color(0xFFB00020);
  static const Color outline = Color(0xFFCED4DA);
}